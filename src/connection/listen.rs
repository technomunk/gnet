//! Server-side connection listener.

/// A listener that processed incoming connectionless packets
/// and accepts them, assigning them a ConnectionId.
pub struct Listener {

}

/// 
pub enum RequestStatus {

}

impl Listener {
}
